output destination oTarget;

input buffer iSound;

node pan nPan;
node gain nGain;

iSound => nPan => nGain => oTarget;

nPan.pan {
  Math.sin(Math.PI * 0.5 * Date.now() * 0.001) * 0.65
}

nGain.gain {
  ((Date.now() * 0.001) % 1.35) / 1.35
}
